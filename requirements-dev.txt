# Зависимости для разработки/CI — не нужны в продакшен-образе (см. Dockerfile,
# который ставит только requirements.txt). Раньше pytest.ini уже настраивал
# asyncio_mode="strict" (нужен pytest-asyncio), а pytest вообще не был нигде
# перечислен как зависимость — "pip install -r requirements.txt && pytest" на
# чистой машине падал с "pytest: command not found".
-r requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.9
