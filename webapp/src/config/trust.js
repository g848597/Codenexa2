// Модуль 5 — Доверие и сообщество. Конфиг-driven, как всё остальное в проекте.
// Правило №1 действует здесь строже, чем где-либо: отзыв без согласия автора,
// отчёт роста за период, которого не было, или SLA "на глаз" — это ровно тот
// обман, который приложение обещало не совершать. Поэтому весь контент этого
// модуля по умолчанию пуст или null, и заполняется только реальными данными.

// --- Отзывы -----------------------------------------------------------
// Публикуется только запись, где consent === true — это явное подтверждение,
// что автор согласился на публикацию именно этого текста под своим именем/ником.
// Пока реальных отзывов нет — массив пуст, это честное состояние, а не заглушка.
export const REVIEWS = [];
// Структура одной записи (для владельца продукта, когда появится первый реальный отзыв):
// {
//   id: 'r-001',
//   authorName: 'Имя Фамилия',       // или null, если автор согласился только на ник
//   authorHandle: '@username',        // или null
//   date: '2026-07-01',               // дата реального отзыва
//   product: 'product-id',            // id продукта из products.js, или null если про экосистему целиком
//   text: '...',                      // текст отзыва как есть, без редактуры смысла
//   consent: true,                    // ставится вручную только после реального согласия
// }

// --- Публичный отчёт роста ---------------------------------------------
// Ежемесячный/ежеквартальный отчёт компании: что получилось, что нет, что дальше.
// Пуст до первого реально прошедшего периода — нельзя отчитаться за период,
// которого ещё не было.
export const GROWTH_REPORTS = [];
// {
//   id: 'gr-2026-q2',
//   period: 'II квартал 2026',
//   publishedDate: '2026-07-01',
//   whatWorked: ['...'],
//   whatDidnt: ['...'],
//   nextFocus: ['...'],
// }

// --- Поддержка -----------------------------------------------------------
export const SUPPORT_INFO = {
  channels: [
    { label: { ru: 'Telegram-бот поддержки', en: 'Telegram support bot' }, url: null }, // TODO(владелец): реальная ссылка на бота/чат поддержки
  ],
  // Среднее время ответа считается по факту за конкретный прошедший период —
  // не заявляется "на глаз". Пока нет посчитанного значения — null, честное
  // "SLA считается" вместо любого правдоподобного числа.
  avgResponseTime: null,   // например: '4 часа'
  measuredPeriod: null,    // например: 'июнь 2026'
  measuredSampleSize: null, // например: 37 — количество обращений, по которым считали
};

// --- Комьюнити -------------------------------------------------------------
export const COMMUNITY = {
  telegramUrl: null,        // TODO(владелец): реальная ссылка на канал/чат
  memberCount: null,        // только если реально получено через Telegram Bot API — иначе null
  memberCountSource: null,  // например: 'Telegram Bot API · getChatMemberCount, обновлено вручную'
};

// --- Статус-страница -------------------------------------------------------
// 'operational' | 'degraded' | 'outage' — обновляется вручную владельцем,
// когда что-то реально происходит. По умолчанию 'operational', потому что
// на момент сборки этого модуля никаких известных инцидентов нет.
export const OVERALL_STATUS = 'operational';

// Реальные инциденты добавляются сюда по мере появления — пусто по умолчанию,
// это нормальное состояние, а не недоработка.
export const STATUS_INCIDENTS = [];
// {
//   id: 'inc-2026-06-14',
//   date: '2026-06-14',
//   title: '...',
//   status: 'investigating', // 'investigating' | 'monitoring' | 'resolved'
//   description: '...',
// }
